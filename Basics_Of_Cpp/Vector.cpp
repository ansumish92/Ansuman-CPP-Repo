#include <iostream>
#include <vector>
using namespace std;

int main() {
    std::vector<int>vec;
    cout << "Size of the vector before data: " << vec.size() << endl;
    cout << "Capacity of the vector before data: " << vec.capacity() << endl;
    return 0;
}